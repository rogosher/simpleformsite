<div class="container">
  <% flash.each do |key, value| %>
    <div class="text-center alert <%= alert_class_for(key) %> alert-dismissible fade in">
      <button type="button" class="close" data-dismiss="alert">&times</button>
      <%= value %>
    </div>
  <% end %>
</div>
